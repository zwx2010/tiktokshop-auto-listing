# W4 code review

Wave: `W4` (task 5)  
Base: `a87c2dd`  
Head: `091d40d`

W4 adds non-publishing probes for Feishu, AI, CDP, and TikTok with explicit
failure results, 24-hour expiry, and configuration-fingerprint invalidation.
The publication gate requires all four probes, human approval, account,
market, rollback plan, and enforces a hard maximum of 50 test products.

Verification: `python -m pytest -q` => **49 passed**; `git diff --check` clean.

## Verdict

**PASS** — the probe and bounded approval-gate requirements are covered.
